{
  "registry": "a2-registry",
  "description": "Central package registry for A2 / Active Oberon community libraries (CPAN/Maven-style). Install by name with `ob get community/<name>` and A2_REGISTRY set.",
  "updated": "2026-07-24",
  "packages": {
    "community/matrix": {
      "status": "validated",
      "modules": 74,
      "path": "packages/matrix",
      "requires": [
        "std/runtime",
        "std/math",
        "std/numerics"
      ],
      "summary": "Dense/complex linear algebra, ML, wavelets, PDE, statistics (Hunziker). Headless numeric core."
    },
    "community/fftw": {
      "status": "validated",
      "modules": 2,
      "path": "packages/fftw",
      "requires": [
        "std/runtime"
      ],
      "native": "libfftw3",
      "summary": "FFI bindings to native FFTW3 (double+single)."
    },
    "community/generics": {
      "status": "validated",
      "modules": 1,
      "path": "packages/generics",
      "requires": [
        "std/runtime"
      ],
      "kind": "generic",
      "summary": "Parametric generic containers (Romanchenko)."
    },
    "community/freeimage": {
      "status": "validated",
      "modules": 1,
      "path": "packages/freeimage",
      "requires": [
        "std/runtime"
      ],
      "native": "libfreeimage",
      "summary": "FFI to native FreeImage (image load/save)."
    },
    "community/usb": {
      "status": "validated",
      "modules": 2,
      "path": "packages/usb",
      "requires": [
        "std/runtime"
      ],
      "native": "libusb / libftd2xx",
      "summary": "FFI to libusb + FTDI FTD2XX."
    },
    "community/v4l2": {
      "status": "pending-manifest",
      "modules": 2,
      "summary": "Video4Linux2 FFI (Unix.V4L2/Unix.libv4l2). Compiles; needs platform-prefixed-filename handling in packaging; GUI VideoExample excluded."
    },
    "community/ph": {
      "status": "pending-deps",
      "modules": 4,
      "summary": "PH utilities (IP monitor, registries, web syndication). Needs std/net + std/crypto + std/base + community SearchTools/NewHTTPClient; IPMonitor uses Windows WSock32."
    },
    "community/opendocument": {
      "status": "pending",
      "modules": 4,
      "summary": "OpenDocument (ODF) codecs. Needs std/web+compress+crypto+base and GUI (WMDialogs); one file (OpenDocumentTransforms) is a malformed XSL-as-source and must be fixed/dropped."
    },
    "community/work": {
      "status": "pending",
      "modules": 44,
      "summary": "Grab-bag of experiments/tools (incl. stray copies of compiler modules); not a coherent library \u2014 triage before packaging."
    },
    "community/octk": {
      "status": "pending-deps",
      "modules": 77,
      "summary": "OCTK compute toolkit. NOT a format problem (ob reads the binary Text fine) \u2014 the intra-package chain (octkBase->octkErrorBase->...) bottoms out in 32-bit modules (I386.octk*Optimized) and FFI to AMD CAL GPU compute. Needs 32-bit target + GPU FFI. 0/77 compile on the 64-bit headless SDK."
    },
    "community/reanimator": {
      "status": "pending-deps",
      "modules": 42,
      "summary": "A JVM (Java) implemented in A2 (JVM*/jjl* = java.lang). Needs legacy module names AosOut/AosProcessors (renamed in current A2) + std/compiler internals (PCT/PCBT) + its own jjl* chain. Needs an Aos* compat shim + std/compiler. 1/42 compile headless."
    },
    "community/sr": {
      "status": "pending-gui",
      "modules": 22,
      "summary": "sr voxel/raytracer engine (binary Text format \u2014 ob reads it). Needs std/gui (WMWindowManager/WMGraphics/Raster) which is not in the headless image. Package once std/gui is installable."
    }
  },
  "notes": {
    "canonical_source": "github.com/sergundo/A2Community",
    "ocp_extras": "a2oberon/ocp adds 33 modules not in A2Community (Rubik, VolumeRaytracer, Wedgelets, Miniscript, ants*, ...) \u2014 union candidates.",
    "binary_format": "CONFIRMED harmless: ob compile/doc READ A2 binary Text format directly (Fox Text subsystem). OCTK/Reanimator/sr fail to build NOT because of the format but because of capability gaps: 32-bit (OCTK I386.*), GPU FFI (OCTK AMD CAL), legacy Aos* names + std/compiler (Reanimator JVM), std/gui (sr). Only our own grep/python text analysis is blocked by the format; use ob doc / .SymUu interface dumps to inspect these instead.",
    "platform_prefixed": "Files like Unix.V4L2.Mod carry a [Platform.] filename prefix; the module inside is V4L2. Packaging must resolve the module name from the MODULE header, not the filename stem.",
    "embedded_excluded": "a2oberon/ARM (73 modules: Enet stack, DMA330, Zynq EMAC) is a bare-metal/embedded track, not a library \u2014 excluded."
  }
}